import React, { useEffect } from 'react'
import { Counter } from 'react-native-date-time-picker'

const App = () => {
  useEffect(() => {
    console.log('')
  })

  return <Counter />
}

export default App
